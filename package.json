{
  "name": "real-estate-report-gen",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w @report-gen/server\" \"npm run dev -w @report-gen/client\"",
    "build": "npm run build -w @report-gen/shared && npm run build -w @report-gen/server && npm run build -w @report-gen/client",
    "db:migrate": "npm run db:migrate -w @report-gen/server",
    "db:generate": "npm run db:generate -w @report-gen/server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.7.0"
  }
}
